@page "/forbidden"
@inject NavigationManager Navigation

<div class="min-h-screen flex flex-col items-center justify-center bg-gray-100 p-4">
    <div class="bg-white shadow-lg rounded-xl p-8 max-w-lg text-center">
        <svg class="w-24 h-24 mx-auto text-red-500 mb-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01M12 21a9 9 0 100-18 9 9 0 000 18z" />
        </svg>

        <h1 class="text-4xl font-bold text-gray-800 mb-2">403 Forbidden</h1>
        <p class="text-gray-600 mb-6">
            Sorry, you don’t have permission to access this page.
        </p>

        <button class="bg-red-500 hover:bg-red-600 text-white font-semibold px-6 py-2 rounded-lg shadow-md transition-colors duration-200"
                @onclick="GoHome">
            Go to Home
        </button>
    </div>

    <p class="mt-6 text-gray-400 text-sm">If you think this is a mistake, contact your administrator.</p>
</div>

@code {
    private void GoHome()
    {
        Navigation.NavigateTo("/");
    }
}
