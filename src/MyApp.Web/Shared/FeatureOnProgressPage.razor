@page "/feature-on-progress"
@inject NavigationManager Navigation

<div class="flex flex-col items-center justify-center min-h-screen bg-gradient-to-br from-indigo-50 to-indigo-100 p-6">
    <!-- Icon / Ilustrasi -->
    <div class="mb-8 animate-bounce">
        <img src="assets/images/under-progress.svg" alt="Under Construction" class="w-48 md:w-64" />
    </div>

    <!-- Judul -->
    <h1 class="text-4xl md:text-5xl font-extrabold text-indigo-800 mb-4 text-center">
        Feature in Progress 🚧
    </h1>

    <!-- Deskripsi -->
    <p class="text-center text-indigo-600 text-lg md:text-xl mb-8 max-w-xl">
        Maaf, fitur ini sedang dalam pengembangan. Sementara, kamu bisa kembali ke halaman utama atau menunggu update
        berikutnya!
    </p>

    <!-- Progress Bar -->
    <div class="w-full max-w-md bg-indigo-200 rounded-full h-4 mb-8 overflow-hidden shadow-inner">
        <div class="bg-indigo-600 h-4 rounded-full animate-pulse" style="width:70%"></div>
    </div>
    <p class="text-indigo-700 mb-8 font-semibold">Progress: 70%</p>

    <!-- Tombol Kembali -->
    <button @onclick="GoHome"
        class="px-6 py-3 bg-indigo-600 hover:bg-indigo-700 text-white rounded-xl shadow-lg transition-all transform hover:scale-105">
        Kembali ke Home
    </button>
</div>

@code {
    private void GoHome()
    {
        Navigation.NavigateTo("/");
    }
}